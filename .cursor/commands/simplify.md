Review the selected code or current file and simplify it:

- Remove unnecessary abstractions and wrapper components
- Reduce nesting depth (flatten conditionals, extract early returns)
- Inline single-use variables and functions
- Remove dead code and unused imports
- Consolidate duplicated logic

Preserve the same behavior, types, and public API. Do not change functionality.
