// Vitest executes server modules in Node, where Next.js's runtime-only marker
// intentionally throws. The alias preserves the import boundary in application
// code while allowing those modules to be unit tested.
